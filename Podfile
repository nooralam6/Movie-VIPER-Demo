# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MovieVIPERDemo' do
  def shared_pods
    pod 'SwiftyVIPER', '~> 1.0'
  end
  use_frameworks!

  # Pods for MovieVIPERDemo
	shared_pods
	pod 'Alamofire'
    pod 'Kingfisher', '~> 5.0'
    pod 'RealmSwift'
    pod 'SwiftMessages'
    pod 'NVActivityIndicatorView'

end
